{
  "name": "shell-1",
  "private": true,
  "type": "module",
  "description": "Discover Linux — Shell 101 atelier in the browser (v86)",
  "scripts": {
    "prepare": "./scripts/prepare-assets.sh",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pack-bundle": "npm run pack-bundle --prefix submodules/v86-runner --",
    "build-state": "npm run build-state --prefix submodules/v86-runner --",
    "build-bundle": "npm run build-bundle --prefix submodules/v86-runner --"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "dompurify": "^3.2.5",
    "marked": "^15.0.11",
    "v86": "^0.5.357"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
